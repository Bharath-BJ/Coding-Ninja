import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";
import App from "./App";

const root = ReactDOM.createRoot(document.getElementById("root"));

root.render(
  <React.StrictMode>
      <App />
  </React.StrictMode>
);


// Task 1 
// To convert the given component code into route based code
// Then to route the CN image to Home page
// Then  to display Oncourse at the top right corner when courses are routed in